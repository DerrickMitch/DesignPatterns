using Singleton;

// used to guaruntee that only one instance of the singleton class exists throughout the entire application, while giving global access to it. 

Singleton.Singleton.Instance().DoSomething();